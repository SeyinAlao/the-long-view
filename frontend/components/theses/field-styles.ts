// One shared class string for every plain text input/textarea across
// the thesis form's sections — defined once so all of them stay in sync
// instead of four section files each keeping their own copy.
export const fieldClass =
  'mt-1 w-full rounded-md border border-ink/20 bg-cream px-3 py-2 text-sm text-ink outline-none focus:border-brass';
